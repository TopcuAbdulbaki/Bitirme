# CUA Node — Python Dependencies
# =====================================================
# Core agent framework
langchain>=0.2.0
langchain-openai>=0.1.0          # LM Studio OpenAI-compat client
langgraph>=0.2.0

# Browser automation (browser-use handles Playwright internally)
browser-use>=0.1.0
playwright>=1.44.0               # browser-use dependency, explicit pin

# gRPC (Orchestrator iletişimi)
grpcio>=1.62.0
grpcio-tools>=1.62.0

# RabbitMQ
pika>=1.3.2

# Utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0

# Production LLM — Qwen3.5-9B (only needed in production mode)
# GPU'suz sistemlerde kaldırılabilir; LM Studio fallback devreye girer
transformers>=4.40.0
torch>=2.2.0
accelerate>=0.28.0
bitsandbytes>=0.43.0             # 4-bit quantization için

# OpenAI Python client (LM Studio local endpoint için)
openai>=1.30.0
