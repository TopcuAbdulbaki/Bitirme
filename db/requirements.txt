# Database Node Requirements

# Core DB & Storage
asyncpg>=0.29.0
minio>=7.2.0
aiohttp>=3.9.0

# gRPC
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf

# Utilities
python-dotenv>=1.0.0
numpy>=1.24.0

# Embeddings (Heavy - ideally should be separate, but logic is coupled currently)
sentence-transformers>=2.2.2
